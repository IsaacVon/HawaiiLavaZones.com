import React, {useState, createContext} from 'react';

// recieves event from price slider drag
// runs handlePriceDrag in contact.jsx

const PriceSliderContext = React.createContext();

export default PriceSliderContext;
